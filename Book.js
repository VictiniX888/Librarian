export default class Book {
    title; description; thumbnail; link;
    constructor(title, description, thumbnail, link) {
        this.title = title;
        this.description = description;
        this.thumbnail = thumbnail;
        this.link = link;
    }
}